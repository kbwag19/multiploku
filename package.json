{
  "name": "multiploku",
  "version": "1.0.0",
  "description": "Testing generateGrid",
  "scripts": {
    "pretest": "[ -d node_modules ] || npm install",
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^29.5.0",
    "jsdom": "^22.1.0"
  }
}
